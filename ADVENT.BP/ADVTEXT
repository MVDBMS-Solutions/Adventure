SUBROUTINE ADVTEXT(XID)
*
* text helper subroutine for Adventure
*
* 4/1985 jdr for CDI
*
PRECISION 0
NEW=''
FOR N = LEN(XID) TO 1 STEP -1
 CHUNK=XID[N,1]
 SEQ=SEQ(CHUNK)-32
 IF SEQ>=0&SEQ<=89 THEN
   NEW:=CHAR(MOD(SEQ+45,90)+32)
 END ELSE NEW:=CHUNK
NEXT N
XID=NEW
RETURN
